"""
CLI subpackage — each subcommand lives in its own module.

  extract.py   → extract / extract-all
  mark.py      → mark / mark-many
  report.py    → status / ls / report
  parser.py    → build_parser() / main()
"""
